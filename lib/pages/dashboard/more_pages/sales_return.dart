import 'package:flutter/material.dart';

class SalesReturn extends StatelessWidget {
  const SalesReturn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Sales Return")),
      body: const Placeholder(),
    );
  }
}
