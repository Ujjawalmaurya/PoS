import 'package:flutter/material.dart';

class DebtNote extends StatelessWidget {
  const DebtNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("DEbit note")),
      body: const Placeholder(),
    );
  }
}
