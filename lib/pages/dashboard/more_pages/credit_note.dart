import 'package:flutter/material.dart';

class CreditNote extends StatelessWidget {
  const CreditNote({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Credit Note")),
      body: const Placeholder(),
    );
  }
}
